[
  {
    "name": "celery",
    "image": "${image}",
    "memory": 300,
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group}",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "web"
      }
    },
    "volumesFrom": [],
    "links": [],
    "mountPoints": [],
    "essential": true,
    "command": ["celery", "--app", "polyledger", "beat", "--loglevel=INFO", "-s", "celerybeat-schedule.db"],
    "environment": [
    ]
  }
]
